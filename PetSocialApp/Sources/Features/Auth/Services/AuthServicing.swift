import Foundation

// Auth now routes through AppSessionStore so the feature can plug directly into
// the shared session lifecycle. This file remains as a stable placeholder for
// future service extraction without breaking feature folder structure.
